# OS
.DS_Store
Thumbs.db
.Spotlight-V100
.Trashes

# Editor
.vscode/
.idea/
*.swp
*.swo

# Logs
logs/

# Python
__pycache__/
*.pyc
*.pyo

# Node
node_modules/
.npm/

# Build artifact (mirror of root, not for Pages)
_dist/

# Dev artifacts (chroma-key / cutout working files) — do NOT commit
_previews/

# Client contracts (private) — do NOT commit
合約/

# Client raw originals (large HEIC straight from the owner's phone).
# Kept in ../_backups/cafechico-client-originals-*.tar instead of bloating the repo.
assets/photos/photo from Conan/
# Editor swap / backup files (prevent accidental secret leaks)
*.md-E
*.md-*
*~
.*.swp
.DS_Store

# Local test wrappers (screenshot harness) — never commit / deploy
_t.html
_test_*.html
